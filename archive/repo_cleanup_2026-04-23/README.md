# Repo Cleanup 2026-04-23

This archive records files moved out of the active runtime path during the
prediction-model cleanup on 2026-04-23.

## safe-delete

Files in `safe-delete/` were no longer referenced by the current app flow or
were local runtime artifacts. They were removed from their original locations.

- `src/views/home/hooks/useCarbonPredictV2.ts`
- `greenfianace_server/uvicorn.stdout.log` if present at cleanup time
- `greenfianace_server/uvicorn.stderr.log` if present at cleanup time

## recommended-archive

Files in `recommended-archive/` were preserved for rollback or historical
comparison, but are not part of the current prediction-page runtime path.

- `greenfianace_server/analysis_models/prediction_model_legacy.py`
- `greenfianace_server/analysis_models/benchmark_reg.py`
- `greenfianace_server/analysis_models/continuous_did.py`
- `greenfianace_server/analysis_models/mediation_effect.py`
- `greenfianace_server/analysis_models/heterogeneity.py`
- `greenfianace_server/analysis_models/robustness_check.py`
- `greenfianace_server/analysis_models/spatial_model.py`
- Old `*_三情景预测结果.csv` outputs generated by the legacy prediction flow

## Notes

- The active prediction pipeline remains:
  `greenfianace_server/analysis_models/prediction_model.py`
- `greenfianace_server/analysis_models/main.py` is now a prediction-focused
  entrypoint. It no longer imports the archived empirical-analysis modules.
- The active API result files remain:
  `组合预测结果.csv`, `STIRPAT三情景预测结果.csv`, `系统动力学情景仿真结果.csv`
- `STIRPAT面板固定效应结果.csv` was intentionally kept in place because the
  frontend custom-tuning flow still depends on it.
- `model_eval/` outputs and the additional STIRPAT ranking/heterogeneity CSVs
  were intentionally kept in the active result directories as research and
  validation artifacts.
